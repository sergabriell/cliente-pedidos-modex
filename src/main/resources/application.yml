server:
  port: ${BACKEND_PORT:8080}
spring:
  application:
    name: 'cliente-pedidos'
  datasource:
    url: jdbc:postgresql://${SQL_DATABASE_SERVER_HOST:localhost}:${SQL_DATABASE_SERVER_PORT:5432}/${SQL_DATABASE_NAME:modex}
    username: ${SQL_DATABASE_USERNAME:postgres}
    password: ${SQL_DATABASE_PASSWORD:postgres123}
    hikari:
      maximum-pool-size: 8
  jpa:
    hibernate:
      ddl-auto: update
jwt:
  secret: "1uHl4mT0NiOmCNosrSaFxl/OVoXkByH9WKfS+d3LVsI="
  expiration: 3600000
app:
  upload:
    base-url: ${BASE_URL:http://localhost:8080/uploads/}
    local-path: ${LOCAL_PATH:/opt/uploads/}